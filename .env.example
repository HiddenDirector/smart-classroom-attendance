# Copy to .env and adjust. Every value here is optional — defaults in config.py.

# --- Flask ---
SECRET_KEY=generate-a-long-random-string
FLASK_ENV=development

# --- Database (defaults to SQLite in ./database/attendance.db) ---
# DATABASE_URL=postgresql://user:pass@localhost/attendance

# --- Camera ---
# Local device index (0 = built-in webcam, 1 = next device e.g. Continuity
# Camera iPhone) OR a phone/IP-camera stream URL:
#   DroidCam:  CAMERA_SOURCE=http://192.168.1.50:4747/video
#   IP Webcam: CAMERA_SOURCE=http://192.168.1.50:8080/video
CAMERA_SOURCE=0
FRAME_WIDTH=1280
FRAME_HEIGHT=720

# --- Motion detection ---
MOTION_MIN_AREA=1500
MOTION_ACTIVE_HOLD_SECONDS=5.0

# --- Face recognition ---
RECOGNITION_CONFIDENCE_THRESHOLD=0.55
RECOGNITION_FRAME_INTERVAL=5
RECOGNITION_MODEL=hog

# --- Long distance / groups ---
# Second higher-resolution detection pass for small, far-away faces.
RECOGNITION_LONG_RANGE=true
RECOGNITION_LONG_RANGE_SCALE=0.5      # raise to 1.0 for max range (more CPU)
RECOGNITION_LONG_RANGE_UPSAMPLE=1     # 2 finds even smaller faces (4x cost)
RECOGNITION_LONG_RANGE_INTERVAL=2     # run it every Nth recognition tick
# For very long range also raise the capture resolution:
# FRAME_WIDTH=1920
# FRAME_HEIGHT=1080

# --- Attendance rules ---
CLASS_START_TIME=09:00
LATE_AFTER_MINUTES=15

# --- Pipeline ---
PIPELINE_AUTOSTART=false

# --- Email reports (optional) ---
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=you@example.com
# SMTP_PASSWORD=app-password
# REPORT_RECIPIENTS=head@example.com,office@example.com
