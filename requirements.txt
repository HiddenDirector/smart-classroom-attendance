# Core web stack
Flask>=3.0
Flask-SQLAlchemy>=3.1
Flask-Login>=0.6
python-dotenv>=1.0

# Computer vision
numpy>=1.26
opencv-python>=4.9
# face_recognition compiles dlib on install — needs cmake + a C++ toolchain.
# macOS:  brew install cmake
# Debian: apt install build-essential cmake libopenblas-dev liblapack-dev
#
# Apple Silicon note: the dlib SOURCE build can fail with a libpng
# "'fp.h' file not found" error. Use the prebuilt wheel instead:
#   pip install dlib-bin
#   pip install face_recognition_models "setuptools<81"
#   pip install --no-deps face_recognition
face_recognition>=1.3.0
face_recognition_models>=0.3.0
# face_recognition_models imports pkg_resources, removed in setuptools >= 81.
setuptools<81

# Excel export (CSV works without it)
openpyxl>=3.1
